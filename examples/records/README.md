# records

这里保存赛后复盘记录。每条记录应只新增，不应覆盖赛前预测文件。

示例命令：

```bash
worldcup-xiaowo record --prediction ./reports/sample-arg-alg.prediction.json --actual-home 3 --actual-away 0 --out ./examples/records/sample-arg-alg.record.json
```
